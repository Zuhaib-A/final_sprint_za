# Use an image with Maven to build the Java application
FROM maven:3.8.4-openjdk-11-slim AS build

# Set the working directory inside the container
WORKDIR /app

# Copy the entire Maven project (including the pom.xml file) into the container
COPY . .

# Build the Maven project
RUN mvn clean install

# Use an image with a Java runtime (e.g., OpenJDK) to run the application
FROM openjdk:11-jre-slim

# Set the working directory inside the container
WORKDIR /app

# Copy the JAR file generated by Maven into the container
COPY --from=build /app/target/backend-app.jar .

# Define the command to run the Java application
CMD ["java", "-jar", "backend-app.jar"]